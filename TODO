# XXX: Look into using Filesys::Ext2 for obtaining extended attributes
# XXX: Look into using git-notes to store metainfo instead of a file in the filesystem
# XXX: Need to determine whether or not missing files need to be handled and how
